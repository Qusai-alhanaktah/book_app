<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta http-equiv="X-UA-Compatible" content="ie=edge">
        <title>Book App</title> 
       </head>
        <body>
         <header>
            <a href="/new">Add a New Book</a>
            <a href="/">Home</a>  
               </header> 
  <ul>
            <img src="<%= book.image_url %>"> 
            <h2><%=book.title %></h2>
            <h3><%=book.author%></h3>
            <p><%=book.description%></p>
            <h4><%=book.isbn%></h4>
            <p><%=book.bookshelf%></p>
            <form action="<%= `/update/${book.id}?_method=PUT`%>" method="POST">
              <input type="text" name="title" value="<%= book.title %>">
              <input type="text" name="contact" value="<%= book.author %>">
              <textarea name="description" cols="30" rows="10" > <%= book.description %></textarea>
              <input type="text" name="status" value="<%= book.isbn %>">
              <input type="text" name="category" value="<%= book.bookshelf %>"> 
              <button type="submit"> Update Details </button>
              </form>
              <form action="<%=`/delete/${book.id}?_method=DELETE`%>" method="POST">
                <button type="submit"> DELETE </button> 
              </form>
</ul>    
<footer>   
</footer>
</body>
</html> 